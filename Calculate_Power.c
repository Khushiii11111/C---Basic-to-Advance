#include <stdio.h>
#include <math.h>

    int main() {
        int a, b;
        a = 2, b = 3;
        int power = pow(a,b);

        printf("power of a: %d", power);
        return 0;
    }