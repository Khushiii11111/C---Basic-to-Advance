#include <stdio.h>

    int main() {

        int a = 10, b = 2;

        int sum = a + b;
        int substraction = a - b;
        int multiply = a * b;
        int division = a/b;

        printf("sum is: %d\n", sum);
        printf("substraction is: %d\n", substraction);
        printf("multiply is: %d\n", multiply);
        printf("division is: %d\n", division); 

        return 0;
    }
